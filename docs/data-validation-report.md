# Data Validation Report

Generated at: 2026-04-27T12:25:06.983Z

## Status

PASSED - no blocking errors found.

## Summary

- Files checked: 11
- Records checked: 11
- Errors: 0
- Warnings: 39

## Warnings

- herio-green / priceListedVnd: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- herio-green / rangeKm: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- herio-green / powerKw: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- herio-green / torqueNm: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- herio-green / batteryKwh: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- herio-green / wheelbaseMm: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- herio-green / seats: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- herio-green / drivetrain: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- limo-green / priceListedVnd: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- limo-green / rangeKm: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- limo-green / powerKw: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- limo-green / torqueNm: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- limo-green / batteryKwh: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- limo-green / wheelbaseMm: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- limo-green / seats: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- limo-green / drivetrain: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- minio-green / priceListedVnd: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- minio-green / rangeKm: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- minio-green / powerKw: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- minio-green / torqueNm: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- minio-green / batteryKwh: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- minio-green / wheelbaseMm: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- minio-green / seats: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- minio-green / drivetrain: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- nerio-green / priceListedVnd: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- nerio-green / rangeKm: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- nerio-green / powerKw: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- nerio-green / torqueNm: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- nerio-green / batteryKwh: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- nerio-green / wheelbaseMm: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- nerio-green / seats: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- nerio-green / drivetrain: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- vf-3 / priceListedVnd: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- vf-5 / priceListedVnd: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- vf-6 / priceListedVnd: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- vf-7 / priceListedVnd: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- vf-8 / priceListedVnd: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- vf-9 / priceListedVnd: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.
- vf-e34 / priceListedVnd: Thông số đang thiếu, UI phải hiển thị trạng thái đang cập nhật.

## Errors

- None

## Governance Notes

- Missing optional specs must render as: Đang cập nhật từ nguồn chính thức.
- Confidence score under 0.6 blocks use as primary technical data.
- Official VinFast sources remain preferred over third-party media.
